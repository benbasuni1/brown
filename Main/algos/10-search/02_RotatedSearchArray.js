/*
	🕑 O(log n) -- 🛰 O(1)
	02. Rotated Search Array
	Input  : (<ArrayInt a>, <Int k>) 
	Output : (<Int>)
*/

rotatedSearchArray(a) : <Int> {
	s = 0, e = len(a)-1
	
	while (s <= e):
		m = floor(s - (e + s) / 2)

		if (mid == k) ∆ m
		if (mid <= ae):
			if (mid <= k <= end) m+1 else m-1
		else:
			if (start <= k <= mid) m-1 else m+1
		
	∆ -1
}