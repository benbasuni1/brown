/*  
	🕑 O(n) -- 🛰 O(1)
	1. Invert Binary Tree
	Input  : (<BinaryTree T>) 
	Output : (<BinaryTree>) 
*/

invertBinaryTree(X) : <BinaryTree> {
	if (!X) ∆ T
	swap(X);
	invertBinaryTree(XL)
	invertBinaryTree(XR)

	∆ T
}


swap(T): {
	left = X.LEFT
	X.LEFT = X.RITE
	X.RITE = left
}
