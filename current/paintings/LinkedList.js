/*
	insertAtHead
		1. (n) -> (h) -> (1)

	insertAtTail
		1. (n)
		2. (c)+
		3. (..) -> (c) -> (n) -> x
*/

class List 
	init(data):
		I.data = data
		I.next = null

	insertAtHead(i, data):
		n = Node(data)
		n -> i
		∆ n

	insertAtTail(i, data): 
		n = Node(data)

		curr = i
		while (curr+) curr++

		curr -> n -> null

		∆ i
