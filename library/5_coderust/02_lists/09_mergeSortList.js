const { List, Helper } = require('./implementation');

// 🕑 O(n log n)
// 🛰 O(n log n)
const mergeSortList = list => {
	
}

const split = (list, firstSecond) => {}
	
const mergeSortedLists = 
const main = () => {
	let a = Helper.createLinkedList([29, 23, 82, 11])
	mergeSortList(a);
}

main()