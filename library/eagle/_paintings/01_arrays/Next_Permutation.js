/*  
	🕑 O(n) -- 🛰 O(1)
	7. Next Permutation
	Input  : (<ArrayInt a>) 
	Output : (<ArrayInt>)
*/

nextPermutation(a): <ArrayInt> {
	i = len(a) - 2
	j = len(a) - 1
	aI, nextI, aJ

	while (i >= 0 && a.i >= next.i) i--;

	if (i >= 0):
		while (j>=0 && a.i >= a.j) j--;
		swap(a.i, a.j)

	∆ reverse(a, i + 1)
}
